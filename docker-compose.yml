version: '3.8'

services:
  web:
    build: .
    volumes:
      - .:/app
      - django_db_data:/app/db_store
    env_file:
      - .env
    ports:
      - "8080:8000"
    restart: always

volumes:
  django_db_data:
